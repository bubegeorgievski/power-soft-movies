export interface MoviesInterface {
    id?: number | null,
    thumbnail: any | null,
    name: String | null,
    description: String | null,
    length: Number | null,
    director: String | null,
    actors: String | null,
    selectedItem: number | null,
}
